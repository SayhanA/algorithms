#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, e, a, b;
    cin >> n >> e;
    int arr[n];
    int groupSize[n];
    for(int i =0; i<n; i++){
        arr[i] = -1;
        groupSize[i] = 1;
    }

    for(int x:groupSize){
        cout << x << endl;
    }
    
    return 0;
}